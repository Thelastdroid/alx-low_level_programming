#include "main.h"


/**
 * _abs - This function computes the 
 * absolute value of an integer
 * @n: the number to check
 * Return: the desired value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
