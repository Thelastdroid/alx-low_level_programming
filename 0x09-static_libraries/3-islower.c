#include "main.h"

/**
 * _islower - This functionn checks for lowercase character
 * @c: the character examined
 * Return: 1 if true, 0 if false
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
